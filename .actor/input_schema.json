{
  "title": "College Recruiter Jobs Scraper",
  "description": "Extract entry-level jobs and internships from College Recruiter. Search by keywords, location, and employment type.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Direct College Recruiter search URL to scrape. If set, overrides other search parameters.",
      "editor": "textfield",
      "example": "https://www.collegerecruiter.com/job-search?keyword=software+engineer&location=California"
    },
    "keyword": {
      "title": "Search Keywords",
      "type": "string",
      "description": "Job title or skills to search (e.g., 'software engineer', 'marketing intern', 'data analyst').",
      "editor": "textfield",
      "prefill": "remote hybrid",
      "default": "",
      "example": "software engineer"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location to search (e.g., 'US', 'California', 'New York, NY').",
      "editor": "textfield",
      "prefill": "US",
      "default": "US",
      "example": "California"
    },
    "employmentType": {
      "title": "Employment Type",
      "type": "string",
      "description": "Filter by employment type.",
      "editor": "select",
      "enum": [
        "All",
        "Full time",
        "Part time",
        "Contractor",
        "Contract to hire",
        "Temporary",
        "Internship"
      ],
      "default": "All",
      "prefill": "All"
    },
    "collectDetails": {
      "title": "Collect Full Job Details",
      "type": "boolean",
      "description": "Visit each job detail page to extract complete descriptions (slower but more detailed).",
      "editor": "checkbox",
      "default": false,
      "prefill": false
    },
    "results_wanted": {
      "title": "Maximum Jobs to Extract",
      "type": "integer",
      "description": "Maximum number of job listings to collect (1-1000).",
      "minimum": 1,
      "maximum": 1000,
      "default": 50,
      "prefill": 50,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum number of result pages to process (10 jobs per page).",
      "minimum": 1,
      "maximum": 100,
      "editor": "number",
      "default": 10,
      "prefill": 10
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum concurrent requests for better performance.",
      "minimum": 1,
      "maximum": 20,
      "default": 10,
      "prefill": 10,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Use Apify Proxy to avoid blocking and ensure reliable data collection.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      },
      "prefill": {
        "useApifyProxy": true
      }
    }
  },
  "required": []
}